# 06 — Cooperative and competitive work

Build on server-authoritative permissions, event ordering and edit leases from task 04. Implement multi-crew membership, 2–4 live/asynchronous participants, crew/host networks, assignment contributions, device control and disconnected practice proposals.

Acceptance:

- All manual role/takeover changes stay within earned permission and are visible. Per-device controller, checkpoint/note normal handoff, 2–10 minute inactivity, 30-second disconnect grace and mutual return agreement work under races.
- Review offline proposals on the current network; show conflicts, retest compatible subsets, and revalidate on commit. Never treat local fork actions as applied shared work.
- Immutable comments/corrections, evidence/file attachment limits, offline opt-in and snapshot history access rules hold after departure/archive/reactivation.
- Competitions freeze format, attempts/ranking, team-size category, result visibility, deadline and upload grace before opening. No new replacement in ranked started teams.
- Clocks and upload grace are distinct. Solution/replay embargo applies to all surfaces and exports. Ranked official vs unranked custom and defect correction rules are enforced.
- Live team disconnection behavior is explicit and fair; untrusted offline reports do not overwrite authoritative shared state.

0.1.0: no backend or simulated real group users. Crew screen states connection is pending.
